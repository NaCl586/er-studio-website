# ER Studio Home Page Asset Patch

Replace `index.html`, `css/home.css`, and `js/home.js` in the existing repository.

The implementation now references the actual assets already present in:
`images/Background.png`, `images/Logo/`, `images/Icon Services/`,
`images/Icon Sosmed/`, `images/Karakter/`, `images/Corousel/`, and
`images/Floodfill banner.png`.

The only intentional placeholder is the top looping gameplay video:
`videos/home-gameplay.mp4`.
